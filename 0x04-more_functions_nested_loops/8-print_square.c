#include "main.h"
/**
 *  * print_square - check the code
 *
 *@size: awwddw
 *
 *    * Return: Always 0.
 */
void print_square(int size)
{
	int i;
	int s;

	if (n <= 0)
	{
		putchar('\n');
	}
	else
	{
		for (i = 0 ; i < n ; i++)
		{
			for (s = 0 ; s < n ; s++)
			{
				putchar(35);
			}
			putchar('\n');
		}
	}

}
